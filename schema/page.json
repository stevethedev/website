{
  "$id": "page.json",
  "$ref": "#/definitions/Page",
  "title": "Page",
  "definitions": {
    "Page": {
      "type": "object",
      "description": "A renderable page",
      "additionalProperties": false,
      "required": ["title", "path", "content"],
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the page"
        },
        "path": {
          "type": "string",
          "description": "The URL path that will be used to access the page, beginning with the leading slash"
        },
        "content": {
          "type": "string",
          "description": "The message to return"
        }
      }
    }
  }
}
